package com.norda.product;

/**
 * WHOLE_BEAN (grano) o molienda ajustada a un metodo de preparacion concreto
 * (seccion 14: si el usuario elige molido, debe indicar para que metodo).
 */
public enum Grind {
    WHOLE_BEAN,
    ESPRESSO,
    MOKA,
    V60,
    FRENCH_PRESS,
    AEROPRESS
}
